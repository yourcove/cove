---
title: Install
description: Install Cove with Docker, native downloads, or from source.
---

# Install Cove

The simplest public path is Docker all-in-one.

```bash
cd docker
docker compose -f docker-compose.allinone.yml up -d
```
